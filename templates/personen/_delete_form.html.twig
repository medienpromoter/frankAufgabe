<form method="post" action="{{ path('app_personen_delete', {'id': personen.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personen.id) }}">
    <button class="btn btn-danger">Mitarbeiter Löschen</button>
</form>
